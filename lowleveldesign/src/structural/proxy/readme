When you need controlled access to an object
When you want to add functionality when accessing an object (logging, lazy loading, etc.)
When you need to control resource usage






Proxy: Controls access to an object by intercepting all requests
Proxy: Typically works with a single object it's standing in for
Proxy: Implements the exact same interface as the object it represents
Choose Proxy when you need to control access to an object






